project('formeson', 'cpp',
  version : '1.3.0',
  default_options : ['warning_level=3',
                     'cpp_std=c++14'])

exe = executable('formeson', 'formeson.cpp',
  install : true)

test('basic', exe)
